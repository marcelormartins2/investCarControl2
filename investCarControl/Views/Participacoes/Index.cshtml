@model IEnumerable<InvestCarControl.Models.Participacao>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PorcentagemCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PorcentagemLucro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parceiro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Veiculo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PorcentagemCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PorcentagemLucro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Parceiro.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Veiculo.Id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
