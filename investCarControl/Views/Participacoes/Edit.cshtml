@model InvestCarControl.Models.Participacao

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Participacao</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ParceiroId" />
            <input type="hidden" asp-for="VeiculoId" />
            <div class="form-group">
                <label asp-for="PorcentagemCompra" class="control-label"></label>
                <input asp-for="PorcentagemCompra" class="form-control" />
                <span asp-validation-for="PorcentagemCompra" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PorcentagemLucro" class="control-label"></label>
                <input asp-for="PorcentagemLucro" class="form-control" />
                <span asp-validation-for="PorcentagemLucro" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
