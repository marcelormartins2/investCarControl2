<link rel="stylesheet" href="/css/style.css" type="text/css" />

<style>
    .container {
        margin: 0px;
        position: fixed;
    }

    .imageBox {
        width: 160px;
        height: 160px;
    }


    .action {
        width: 600px;
        height: 30px;
        margin: 0px 0;
    }

    .cropped > img {
        margin-right: 10px;
    }
    #centro {
        width: 138px;
        height: 138px;
        position: absolute;
        top: 6%;
        left: 6%;
    }

</style>
<base href="/img/avatars/">

<table>
    <tr>
        <td>            
            <div class="container">
                <div class="imageBox" style="resize:both; overflow:auto;">
                    <img src="circulo.png" alt="Avatar" id="centro">
                    <div class="spinner" style="display: none;">Carregando...</div>
                </div>
                <div class="action">
                    <a style="font-size: 0.5rem;">
                        <i class="fa fa-plus-square-o fa-4x" id="btnZoomIn" title="zoom in">&nbsp</i>
                        <i class="fa fa-minus-square-o fa-4x" id="btnZoomOut" title="zoom out">&nbsp</i>
                        <i class="fa fa-check-square-o fa-4x" id="btnCrop" title="Salvar">&nbsp</i>
                        <label for="file">
                            <i class="fa fa-refresh fa-4x" title="Trocar foto">&nbsp</i>
                        </label>
                        <input id="file" type="file" accept="image/*" style="display:none" />
                        <i class="fa fa-trash-o fa-4x" id="btnCancel" title="Cancelar"></i>
                    </a>


                </div>
                <div class="cropped"> </div>
            </div>
        </td>
    </tr>
</table>

@section scripts
{


    <script type="text/javascript">
        $(window).load(function () {
            var options =
            {
                thumbBox: '.thumbBox',
                spinner: '.spinner',
                imgSrc: 'default.png'
            }
            var cropper = $('.imageBox').cropbox(options);
            $('#file').on('change', function () {
                var reader = new FileReader();
                reader.onload = function (e) {
                    options.imgSrc = e.target.result;
                    cropper = $('.imageBox').cropbox(options);
                }
                reader.readAsDataURL(this.files[0]);
                this.files = [];
            })
            $('#btnCrop').on('click', function () {
                var img = cropper.getAvatar()
                $('.cropped').append('<img src="' + img + '">');
            })
            $('#btnZoomIn').on('click', function () {
                cropper.zoomIn();
            })
            $('#btnZoomOut').on('click', function () {
                cropper.zoomOut();
            })
        });
    </script>
}